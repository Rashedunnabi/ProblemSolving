{
	"Bit_mask": {
		"prefix": "BitMasking",
		"body": [
		  "",
		  "    int bt = coins.size();",
		  "    int sz = 1 << bt;",
		  "    for (int i = 0; i < sz; i++)",
		  "    {",
		  "        for (int mask = 0; mask <= bt; mask++)",
		  "",
		  "            if ((i >> mask) & 1 != 0)",
		  "            {",
		  "                // condition",
		  "            }",
		  "    }"
		],
		"description": "Bit_mask"
	  }
}